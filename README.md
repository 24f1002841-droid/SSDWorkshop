Spec driven development workshop

## Python GUI Calculator

This repo includes a simple desktop calculator built with Python `tkinter`.

Run it:

```bash
python -m calculator
```

If `tkinter` is missing on your Linux system, install it (package name is distro-dependent; commonly `python3-tk`).